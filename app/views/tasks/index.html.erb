<h1>My tasks</h1>
<div>
  <%= link_to 'Create a new task', new_task_path %>
  <ul>
    <% @tasks.each do |task| %>
    <li><input type="checkbox"> <%= link_to task.title, task_path(task) %> </li>
    <% end %>
  </ul>
</div>
